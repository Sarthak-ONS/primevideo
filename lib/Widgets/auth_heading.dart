import 'package:flutter/material.dart';

buildAuthHeading(String title) => Text(
      title,
      style: const TextStyle(
        color: Colors.white,
        fontWeight: FontWeight.bold,
        fontSize: 27,
      ),
    );
