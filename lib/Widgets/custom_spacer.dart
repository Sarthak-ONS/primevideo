import 'package:flutter/cupertino.dart';

buildHeightSizedBox({double? height = 10}) => SizedBox(
      height: height,
    );
buildWidthSizedBox({double? width = 10}) => SizedBox(
      width: width,
    );
